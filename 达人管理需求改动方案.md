# 达人管理需求 — 源码改动方案（修正版 v2）

> 基于 `newcps-crm-frontend` + `newcps` 源码，标注每个需求对应页面、文件路径、当前代码位置、具体改动点。
> 涉及前后端代码，所有改动均指向实际源码。

---

## 需求 1：入库时自动识别平台最简 URL 并解析（IG / YT / TT）

### 涉及页面 & 文件

有两个弹窗共用了同一套 URL 解析逻辑，需同时修改：

| 弹窗 | 源码文件 | 行号 |
|------|---------|------|
| **新建达人弹窗** | `src/pages/Members/Koc/components/CreateTalentNewModal/TalentInfoForm/config.tsx` | :215-446 |
| **新建线索弹窗** | `src/pages/Members/Koc/components/CreateThreadModal/schema.tsx` | :137-260（URL 输入区域） |

### 当前代码分析

两处均使用相同的流程：

1. URL 输入框 (`Input` + `onBlur`)
2. `onBlurHandle` → `setTalentInfo()` 或 `getAccountInfo()`
3. 调后端 API `/remaiSupport/getAccountInfo` 解析平台类型 & 粉丝数

**问题**：前端无本地 URL 预识别，粘贴链接后必须等待 API 返回才知道平台是否支持。

### 改动点

#### 1. 在 `config.tsx` 和 `schema.tsx` 中分别新增 URL 规则映射表

在文件顶部（约 20-40 行位置）新增：

```typescript
// 新增 —— URL 格式自动识别规则（IG / YT / TT）
const PLATFORM_URL_RULES = [
  {
    code: 8,
    name: 'Instagram',
    domains: ['instagram.com', 'instagr.am'],
    desc: '支持 instagram.com/{username}、instagr.am/{username}、/reel/、/p/ 格式',
  },
  {
    code: 9,
    name: 'YouTube',
    domains: ['youtube.com', 'youtu.be'],
    desc: '支持 youtube.com/@{handle}、/c/{name}、/channel/{id}、youtu.be/{id} 格式',
  },
  {
    code: 10,
    name: 'TikTok',
    domains: ['tiktok.com', 'douyin.com'],
    desc: '支持 tiktok.com/@{username}、douyin.com/user/{id} 格式',
  },
];
```

#### 2. 修改 `onBlurHandle`，新增前端预校验

**config.tsx 当前代码（:402-404）**：
```typescript
const onBlurHandle = () => {
  setTalentInfo();
};
```

改为：
```typescript
const onBlurHandle = () => {
  const url = getFieldValue(['data', index, 'url']);
  if (!url || !isUrl(url)) return;
  const matched = PLATFORM_URL_RULES.find(rule =>
    rule.domains.some(domain => url.includes(domain))
  );
  if (matched) {
    // 前端预识别后继续调 API 获取详情
    message.info(`识别到 ${matched.name} 平台链接，正在获取达人信息...`);
  } else {
    message.warning(`暂不支持此平台链接。当前支持：Instagram、YouTube、TikTok\n支持的格式：\n${PLATFORM_URL_RULES.map(r => `• ${r.name}: ${r.desc}`).join('\n')}`);
    return;
  }
  setTalentInfo();
};
```

**schema.tsx 同理修改（:168-170）**：
```typescript
const onBlurHandle = () => {
  // 同样加入前端 URL 预识别逻辑
  const url = getFieldValue('url');
  if (!url || !isUrl(url)) return;
  const matched = PLATFORM_URL_RULES.find(rule =>
    rule.domains.some(domain => url.includes(domain))
  );
  if (!matched) {
    message.warning('暂不支持此平台链接。当前支持：Instagram、YouTube、TikTok');
    return;
  }
  setTalentInfo();
};
```

### 后端配合
无需。API `/remaiSupport/getAccountInfo` 已有。

---

## 需求 2：达人库列表支持搜索无负责人的数据

### 涉及页面 & 文件

达人库列表页 — 搜索区

```
src/pages/Members/Koc/TalentTree/tableSchema.tsx
```

### 当前代码分析

达人库搜索区有两个「搜达人」的筛选项，它们是**且**关系：

| 搜索字段 | dataIndex / key | 文件行号 | 说明 |
|---------|----------------|---------|------|
| **达人ID** | `id` → transform to `ids` | :973-989 | `hideInTable: true`, `fillBack: true` |
| **平台账号** | `platform_account` → transform to `platform_account` | :990-1008 | `hideInTable: true`, `renderFormItem: '{{platformAccountFormItem}}'` |
| **负责人** | `agent_list` / `agent_id` | :862-912 | `registerToSearch: true`，已有 DmCascader 选择器 |

「负责人」搜索字段（:862-912）已有 DmCascader 选择具体负责人。**需要在这两个搜索字段上方或旁边增加「无负责人」的勾选项**，以便用户同时通过达人ID/平台账号+无负责人组合搜索。

### 改动点

#### 在 `tableSchema.tsx` 搜索栏底部新增一个独立的「负责人状态」搜索项（约 1008 行后）

不直接修改达人ID/平台账号字段本身，而是新增一个独立的搜索项与它们并列：

```typescript
// 在 平台账号 搜索项后新增 —— 负责人状态筛选
{
  title: '负责人状态',
  width: 100,
  hideInTable: true,
  key: 'agent_status',
  registerToSearch: true,
  order: talentTreeOrderMap.get('负责人状态'),
  renderFormItem: () => (
    <Radio.Group>
      <Radio value="">全部</Radio>
      <Radio value="has">有负责人</Radio>
      <Radio value="none">无负责人</Radio>
    </Radio.Group>
  ),
  search: {
    transform: (value: string) => {
      if (value === 'none') return { agent_id: '', agent_type: undefined };
      if (value === 'has') return { has_agent: '1' };
      return {};
    },
  },
},
```

#### 在 `TalentTreeList.tsx` 的 `listRequest` 中处理新参数（:147-148）

当前：
```typescript
params.agent_type = params?.agent_id?.type;
params.agent_id = params?.agent_id?.id || undefined;
```

改为：
```typescript
// 处理负责人状态搜索
if (params?.has_agent === '1') {
  // has_agent 参数已传递，无需额外处理
} else if (params?.agent_id === '') {
  // 空字符串表示搜索无负责人
  params.agent_id = '';
  params.agent_type = undefined;
  params.has_agent = undefined;
} else {
  params.agent_type = params?.agent_id?.type;
  params.agent_id = params?.agent_id?.id || undefined;
}
params.has_agent = params?.has_agent || undefined;
```

### 后端配合

| 接口 | 改动 |
|------|------|
| `GET /crmV2/agentList` | 新增参数 `has_agent=1` 过滤有负责人；`agent_id=`(空串) 过滤无负责人 |

---

## 需求 3：达人库数据支持跨系统交接负责人

### 涉及范围

需要支持跨系统交接的功能点：

- 达人库（达人库列表 → 批量操作 → 交接）
- 达人线索（线索库 → 批量操作 → 分配/交接）
- 社群库
- 网站媒体

### 源码文件

交接功能已存在，核心入口：

```
src/pages/Members/Koc/TalentTree/TalentTreeList.tsx     → 达人库批量交接
src/pages/Members/Koc/components/DistributeThreadModal.tsx  → 交接弹窗
```

### 当前代码

**TalentTreeList.tsx:545-564** — 批量操作 Dropdown 中有「批量交接」按钮，调 `batchSelect(ConstructType.JOIN)` → `DistributeThreadModal`。

当前交接仅在**同系统内**流转（CRM 库内转移负责人）。

### 改动点

#### 1. DistributeThreadModal 中增加「目标系统」选择

在交接表单中新增 Radio：

```typescript
// DistributeThreadModal.tsx 新增
const TARGET_SYSTEMS = [
  { value: 'dm', label: 'DM（多麦联盟）' },
  { value: 'kn', label: 'KN（KreatorNow）' },
  { value: 'lkb', label: 'LKB（Linkbest）' },
];

// 表单内新增
<Form.Item label="目标系统" name="target_system" rules={[{ required: true, message: '请选择目标系统' }]}>
  <Radio.Group>
    {TARGET_SYSTEMS.map(sys => (
      <Radio key={sys.value} value={sys.value}>{sys.label}</Radio>
    ))}
  </Radio.Group>
</Form.Item>
```

#### 2. 提交参数增加 `target_system`

```typescript
// 提交时
const params = {
  talent_ids: currentList.current.map(i => i.id).join(','),
  target_admin_id: targetAdminId,
  target_system: targetSystem,  // 新增
  source_system: 'talent_tree', // 标识来源
};
```

### 后端配合

| 接口 | 改动 |
|------|------|
| `POST /crmV2/transferTalent` | 新增 `target_system` 字段（dm/kn/lkb） |
| 交接逻辑 | 根据 `target_system` 不同调用不同系统的写入 API |

**注意**：交接功能本身已有，本次只需扩展目标系统选项。交接的记录需入库以便追溯。

---

## 需求 4 + 需求 6：我的达人库提报前/达人列表透出招募任务进度

这两个需求合并实现，因为都是围绕「达人 + 招募任务」的信息展示。

### 涉及页面 & 文件

| 页面 | 前端文件 | 后端文件 |
|------|---------|---------|
| **我的达人库 - 提报弹窗** | `src/pages/Members/Koc/components/PutForwardModal.tsx` | `app/common/models/DmAdsProjectSubmitLog.php` |
| **达人库列表** | `src/pages/Members/Koc/TalentTree/tableSchema.tsx` | `app/common/models/DmAdsProject.php` |
| **线索库列表** | `src/pages/Members/Koc/ThreadedTree/tableSchema.tsx` | — |

### 招募任务完整状态流转（已更新状态机）

根据后端模型 `DmAdsProjectSubmitLog`，招募任务的完整流程状态及进度百分比：

```
阶段       状态码    说明                    进度%
──────────────────────────────────────────────
提报阶段    20       待提报审批              15%
           22       提报通过                30%

报价阶段    30       待报价审批              45%
           32       报价通过                60%

执行阶段    40       确认合作，待执行        75%
           51       执行中                  90%
           52       已完结                  100%
```

### 进度百分比算法（已更新）

```
function calcProgress(statusCode: number): { percent: number; label: string; phase: string } {
  if (statusCode >= 52) return { percent: 100, label: '已完结', phase: '完结' };
  if (statusCode >= 51) return { percent: 90, label: '执行中', phase: '执行' };
  if (statusCode >= 40) return { percent: 75, label: '确认合作，待执行', phase: '执行' };
  if (statusCode >= 32) return { percent: 60, label: '报价通过', phase: '报价' };
  if (statusCode >= 30) return { percent: 45, label: '待报价审批', phase: '报价' };
  if (statusCode >= 22) return { percent: 30, label: '提报通过', phase: '提报' };
  if (statusCode >= 20) return { percent: 15, label: '待提报审批', phase: '提报' };
  // 拒绝/终止状态
  if ([11, 21, 23, 31, 33].includes(statusCode)) return { percent: 0, label: '已终止', phase: '终止' };
  return { percent: 0, label: '未知', phase: '未知' };
}
```

**流量主端合作状态聚合**（来自 `DmAdsProjectSubmitLog.DISPLAY_STATUS_MAPPING`）：
- `-1` 未申请 | `0` 待审核 | `1` 拒绝 | `2` 待确认合作 | `3` 合作中 | `4` 完结

### 改动点 A：PutForwardModal 提报弹窗（需求 4）

**PutForwardModal.tsx** 打开时获取该达人与此招募任务关联的业务线 + 合作状态：

```typescript
// PutForwardModal.tsx 新增接口和数据状态
interface TaskBizLineInfo {
  task_name: string;           // 招募任务名称
  business_line_name: string;  // 关联业务线
  submit_status: number;       // 提报状态码
  progress: number;            // 进度百分比
  phase_label: string;         // 阶段名称
}

const [taskBizLines, setTaskBizLines] = useState<TaskBizLineInfo[]>([]);

useEffect(() => {
  if (visible && record?.raw_talent_id) {
    // 调接口获取该达人关联的招募任务 + 业务线 + 合作状态
    request('/crmV2/talentRecruitmentTaskStatus', {
      params: { raw_talent_id: record.raw_talent_id }
    }).then(res => {
      if (res.f === 1) {
        setTaskBizLines((res.d || []).map((item: any) => ({
          ...item,
          progress: calcProgress(item.submit_status).percent,
          phase_label: calcProgress(item.submit_status).label,
        })));
      }
    });
  }
}, [visible, record?.raw_talent_id]);
```

**弹窗顶部新增展示区域**：

```tsx
{taskBizLines.length > 0 && (
  <Alert
    type="warning"
    showIcon
    message="该达人已有相关联的招募任务"
    description={
      <Table
        dataSource={taskBizLines}
        pagination={false}
        size="small"
        columns={[
          { title: '招募任务', dataIndex: 'task_name' },
          { title: '业务线', dataIndex: 'business_line_name' },
          {
            title: '合作状态',
            dataIndex: 'phase_label',
            render: (text, record) => (
              <Space>
                <Tag color={record.progress >= 100 ? 'green' : record.progress >= 50 ? 'geekblue' : 'orange'}>
                  {text}
                </Tag>
                <Progress percent={record.progress} size="small" style={{ width: 80 }} />
              </Space>
            ),
          },
        ]}
      />
    }
    style={{ marginBottom: 16 }}
  />
)}
```

### 改动点 B：达人库/线索库列表新增列（需求 6）

#### TalentTree/tableSchema.tsx 新增列（约 1008 行后）

```typescript
{
  title: '招募任务',
  dataIndex: 'recruitment_tasks',
  width: 320,
  key: 'recruitment_tasks',
  hideInSearch: true,
  render: (value: any[], record: any) => {
    if (!record?.recruitment_tasks?.length) return '-';
    return (
      <div>
        {record.recruitment_tasks.map((task: any, i: number) => {
          const p = calcProgress(task.submit_status);
          return (
            <div key={i} style={{
              marginBottom: i > 0 ? 8 : 0, padding: '6px 0',
              borderBottom: i < record.recruitment_tasks.length - 1 ? '1px dashed #f0f0f0' : 'none'
            }}>
              <div style={{ fontWeight: 500, fontSize: 13, marginBottom: 4 }}>
                {task.task_name}
                <span style={{ marginLeft: 8, fontSize: 12, color: 'rgba(0,0,0,0.45)' }}>
                  {task.business_line_name}
                </span>
              </div>
              <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
                <Progress
                  percent={p.percent}
                  size="small"
                  status={p.percent >= 100 ? 'success' : 'active'}
                  style={{ flex: 1, maxWidth: 160 }}
                />
                <Tag color={
                  p.percent >= 100 ? 'green' :
                  p.percent >= 50 ? 'geekblue' :
                  p.percent > 0 ? 'orange' : 'default'
                }>{p.label}</Tag>
              </div>
            </div>
          );
        })}
      </div>
    );
  },
},
```

#### ThreadedTree/tableSchema.tsx 新增相同的列（约 811 行前）

```typescript
{
  title: '招募任务',
  dataIndex: 'recruitment_tasks',
  width: 320,
  key: 'recruitment_tasks',
  hideInSearch: true,
  render: (value: any[], record: any) => {
    // 与上方相同的 render 逻辑
    if (!record?.recruitment_tasks?.length) return '-';
    // ...
  },
},
```

#### 列表请求接口需返回 recruitment_tasks 数据

**TalentTreeList.tsx `listRequest`（:161-178）** 当前请求 `/crmV2/agentList`：

```typescript
const res = await request('/crmV2/agentList', { method: 'GET', params, ...options });
// 在返回后、处理 handleData 前，批量查询招募任务数据
if (res?.d?.Results?.length && !params?.is_export) {
  const talentIds = res.d.Results.map(r => r.id).join(',');
  const taskRes = await request('/crmV2/batchRecruitmentTasks', {
    params: { talent_ids: talentIds, type: 'talent' }
  });
  if (taskRes.f === 1) {
    const taskMap = taskRes.d || {};
    res.d.Results.forEach(item => {
      item.recruitment_tasks = taskMap[item.id] || [];
    });
  }
}
```

**ThreadedTree/index.tsx `listRequest`（:150-162）** 同理，请求 `/crmV2/list` 后批量查询。

### 后端新增接口

| 接口 | 方法 | 参数 | 返回 |
|------|------|------|------|
| `GET /crmV2/talentRecruitmentTaskStatus` | GET | `raw_talent_id` | 该达人关联的招募任务列表，含 task_name, business_line_name, submit_status |
| `GET /crmV2/batchRecruitmentTasks` | GET | `talent_ids`(逗号分隔), `type`(talent/clue) | Map<talent_id, Array<{task_name, business_line_name, submit_status, deadline}>> |

---

## 需求 7：全部线索增加「跟进人」筛选项

### 涉及页面 & 文件

全部线索 → 搜索区

```
src/pages/Members/Koc/ThreadedTree/tableSchema.tsx
```

### 当前代码

**ThreadedTree/tableSchema.tsx** 中 `schemaTableProps` 部分（:692-697）：

```typescript
{
  title: '跟进人',
  dataIndex: 'follow_up_person_name',
  width: 100,
  key: 'follow_up_person_name',
  // 缺少 registerToSearch: true
  // 缺少 renderFormItem / search.transform
},
```

当前「跟进人」在搜索区**不可见**，仅作为列表展示列。

但在同一文件的 `ThreadedTreeColumns` 导出中（:326-360），已配好完整的搜索配置：
```typescript
{
  title: '跟进人',
  dataIndex: 'follow_up_person_name',
  width: 100,
  key: 'follow_up_person_name',
  registerToSearch: true,
  order: threadedTreeOrderMap.get('跟进人'),
  renderFormItem: () => (
    <DmCascader
      searchUrl="/department/medium"
      param={{ pid: '1017' }}
      rq={request}
      changeOnSelect
      placeholder="请选择跟进人"
      customDefaultHandle={(item) => {
        if (String(item?.status) === '0' && String(item.type) === '2') {
          item.name += '(已离职)';
        }
      }}
      moreReturn={{ type: 'type' }}
      showCheckedStrategy={SHOW_CHILD}
      showArrow
    />
  ),
  search: {
    transform: (value: any) => ({
      follow_up_person_id: value?.id,
      follow_up_person_type: value?.type,
    }),
  },
},
```

### 改动点

将 `schemaTableProps` 中「跟进人」列（:692-697）替换为与 `ThreadedTreeColumns` 一致的配置：

```typescript
{
  title: '跟进人',
  dataIndex: 'follow_up_person_name',
  width: 100,
  key: 'follow_up_person_name',
  registerToSearch: true,                    // 新增
  order: threadedTreeOrderMap.get('跟进人'),   // 新增
  renderFormItem: () => (                     // 新增
    <DmCascader
      searchUrl="/department/medium"
      param={{ pid: '1017' }}
      rq={request}
      changeOnSelect
      placeholder="请选择跟进人"
      customDefaultHandle={(item) => {
        if (String(item?.status) === '0' && String(item.type) === '2') {
          item.name += '(已离职)';
        }
      }}
      moreReturn={{ type: 'type' }}
      showCheckedStrategy={SHOW_CHILD}
      showArrow
    />
  ),
  search: {                                   // 新增
    transform: (value: any) => ({
      follow_up_person_id: value?.id,
      follow_up_person_type: value?.type,
    }),
  },
},
```

**效果**：全部线索搜索区的现有搜索项下方出现「跟进人」筛选器，使用系统标准的 `DmCascader` 组件（同 ThreadedTreeColumns 中的配置），支持按组织架构选择跟进人。

**组件样式参考**：达人库筛选区「标签」组件（`NewLabelSelectCascader`），位于 `src/pages/Members/Koc/TalentTree/tableSchema.tsx:967-983`，底层使用 `DmCascader`，接口 `GET /label/getLabelTreeList`，params: `{ application_targets: 7 }`。跟进人筛选器复用相同的 DmCascader 组件模式，搜索接口改为 `/department/medium`。

### 后端配合

无需改动。API `GET /crmV2/list` 已支持 `follow_up_person_id` 和 `follow_up_person_type` 参数（:146-147）。

---

## 需求 5：达人公海自动回收机制

### 涉及页面 & 文件

| 页面 | 前端文件 | 后端文件 |
|------|---------|---------|
| **达人库列表** | `src/pages/Members/Koc/TalentTree/tableSchema.tsx` | `app/common/models/DmAdserCrm.php` |
| **我的达人库列表** | `src/pages/Members/MyDarenLibrary/components/tableSchema.tsx` | `app/common/service/RawTalent/RawTalentService.php` |
| 定时任务 | — | 新增 `bin/worker/DropToPublicPoolTask.php` |

### 改动点概要

#### 1. 国内平台达人永不掉公海

判定依据：达人平台分类字段（`platform_category`），国内平台（抖音、小红书、微博、B站等）不参与掉公海逻辑，仅海外平台执行。

#### 2. 判定逻辑变更：入库后5个月 → 近5个月

**旧逻辑**：从入库（有负责人）之日起算，5个月内是否有提报通过（status ≥ 22）记录。

**新逻辑**：每次定时任务扫描时，**检查达人近5个月内是否有提报通过（status ≥ 30）记录**，而非从入库时间起算固定5个月窗口。

#### 3. 首次成为负责人

首次私有一个达人（从公海私有到名下）：**需满5个月后再计算近5个月**是否有合作。即：
- 负责人首次成为负责人时间 + 5个月（等待期）
- 到期后检查近5个月是否有提报通过（status ≥ 30）记录
- 无 → 掉公海
- 有 → 保留

#### 4. 非首次私有（反复私有）

**剔除永久不可私有逻辑**：去掉原有的「第二次掉公海→永久不可私有」的限制。达人掉公海后，任意媒介（含原媒介）均可反复私有该达人，不受次数限制。

**非首次私有校验**：只要不是首次私有，都是校验**1个月内是否有合作**。即：
- 最近一次私有时间 + 1个月
- 到期后检查近1个月是否有提报通过（status ≥ 30）记录
- 无 → 掉公海
- 有 → 保留

#### 5. 列表末位透出掉公海倒计时

在【达人库】【我的达人库】列表末位新增「掉公海倒计时」列，展示该达人距离掉公海剩余天数。

**倒计时计算规则**：
- **首次私有**：负责人首次成为负责人时间 + 5个月 + 5个月 - 当前时间 = 剩余天数
- **非首次私有**：最近一次私有时间 + 1个月 - 当前时间 = 剩余天数
- **国内平台达人**：显示「—」（不掉公海）
- 支持按倒计时排序

### 数据字段设计

| 字段 | 说明 | 所在表 |
|------|------|--------|
| `platform_category` | 平台分类（国内/海外），用于判定是否执行掉公海 | 达人基础表 |
| `first_agent_time` | 首次成为负责人时间（用于计算5个月窗口起始） | 达人扩展表 |
| `last_private_time` | 最近一次被私有时间（用于计算1个月窗口） | 达人扩展表 |
| `last_drop_time` | 最近一次掉公海时间 | 达人扩展表 |
| `private_count` | 该达人被私有次数（用于判断首次/非首次私有） | 媒介-达人关系表 |
| `cooperation_status` | 合作状态（用于判断临界点） | `DmAdsProjectSubmitLog` |
| `drop_countdown_days` | 掉公海倒计时天数（前端计算/后端返回，用于列表展示） | 返回字段 |

### 后端定时任务

```php
class DropToPublicPoolTask extends Worker {
  function main() {
    1. 查询海外平台(platform_category=海外)且有负责人的达人
    2. 首次私有：负责人时间>=5个月 且 近5个月无提报通过(status>=30)记录→掉公海
    3. 非首次私有：私有时间>=1个月 且 近1个月无提报通过(status>=30)记录→掉公海
    4. 国内平台达人跳过，不参与掉公海
    5. 掉公海后记录私有时长、私有次数，不设永久锁定
    6. 返回列表时计算 drop_countdown_days 前端展示倒计时
  }
}
```

### 前端改动

#### TalentTree/tableSchema.tsx 新增「掉公海倒计时」列

```typescript
{
  title: '掉公海倒计时',
  dataIndex: 'drop_countdown_days',
  width: 120,
  key: 'drop_countdown_days',
  hideInSearch: true,
  sorter: true,
  render: (value: number, record: any) => {
    // 国内平台达人
    if (record?.platform_category === 'domestic') return h('span', { className: 'text-muted' }, '—');
    // 无值
    if (value === null || value === undefined) return h('span', { className: 'text-muted' }, '-');
    // 已过期
    if (value <= 0) return h('span', { style: { color: '#cf1322', fontWeight: 600 } }, `已超期${Math.abs(value)}天`);
    // 正常倒计时
    return h('span', { style: { color: value <= 7 ? '#fa8c16' : '#4e5969' } }, `${value}天`);
  },
},
```

#### MyDarenLibrary/components/tableSchema.tsx 同理新增相同列

---

| # | 需求 | 前端文件 | 后端接口 | 改动规模 |
|---|------|---------|---------|---------|
| 1 | URL 自动识别 | `TalentInfoForm/config.tsx` + `CreateThreadModal/schema.tsx` | 无需 | 小（两处各 +20 行） |
| 2 | 无负责人搜索 | `TalentTree/tableSchema.tsx` + `TalentTreeList.tsx` | `GET /crmV2/agentList` 加 `has_agent` 参数 | 小 |
| 3 | 跨系统交接 | `DistributeThreadModal.tsx` | `POST /crmV2/transferTalent` 加 `target_system` | 中（交接逻辑扩展） |
| 4+6 | 招募任务进度（状态机更新） | `PutForwardModal.tsx` + `TalentTree/tableSchema.tsx` + `ThreadedTree/tableSchema.tsx` | 新增 `GET /crmV2/batchRecruitmentTasks`、`GET /crmV2/talentRecruitmentTaskStatus` | 大（前后端新增） |
| 5 | 公海回收机制 | `TalentTree/tableSchema.tsx` + `MyDarenLibrary/components/tableSchema.tsx`（新增倒计时列） | 新增 `bin/worker/DropToPublicPoolTask.php` 定时任务 | 大（后端逻辑重构 + 前端新增列） |
| 7 | 跟进人筛选（组件参考：标签筛选器） | `ThreadedTree/tableSchema.tsx` | 无需（API 已支持） | 小（+15 行） |
